/node_modules
.env
.env.*
*.env
*.env.*
!.env.example
!*.env.example


/generated/prisma

# Runtime upload storage — served from the `backend_uploads` Docker volume in
# production, so the contents must never be committed.
/uploads/
